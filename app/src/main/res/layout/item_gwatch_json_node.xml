<?xml version="1.0" encoding="utf-8"?>
<!-- One entry of the config tree. The leading spacer is widened per nesting level by the
     adapter, so indentation does not need a layout per depth. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:baselineAligned="false"
    android:gravity="center_vertical"
    android:minHeight="48dp"
    android:orientation="horizontal"
    android:paddingStart="4dp"
    android:paddingEnd="4dp">

    <View
        android:id="@+id/gwatch_json_indent"
        android:layout_width="0dp"
        android:layout_height="1dp" />

    <TextView
        android:id="@+id/gwatch_json_chevron"
        android:layout_width="24dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="?android:attr/textColorSecondary"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/gwatch_json_key"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:fontFamily="monospace"
        android:maxLines="1"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="13sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/gwatch_json_value"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:fontFamily="monospace"
        android:maxLines="1"
        android:textColor="?android:attr/textColorSecondary"
        android:textSize="13sp" />

    <ImageButton
        android:id="@+id/gwatch_json_menu"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/gwatch_json_entry_actions"
        android:src="@drawable/ic_more_vert"
        app:tint="?android:attr/textColorSecondary"
        xmlns:app="http://schemas.android.com/apk/res-auto" />

</LinearLayout>
